# 02. 기능명세서
## 일본 골프 여행 가이드 웹앱

# 1. 기술 원칙

최우선 순위:

1. 속도
2. 단순함
3. 관리 용이성
4. 정확성
5. 모바일 UX

Google Sheets는 CMS 저장소로 사용한다.

고객 페이지에서 Google Sheets API를 직접 호출하지 않는다.

---

# 2. 데이터 흐름

```text
Google Sheets
   ↓
서버 데이터 레이어
   ↓
캐시
   ↓
고객 웹앱
```

관리자:

```text
관리자 페이지
   ↓
서버 API
   ↓
Google Sheets 업데이트
   ↓
캐시 무효화
```

---

# 3. 캐시

권장:

- 고객 조회 캐시: 10~30분
- 관리자 저장 성공 시 관련 캐시 즉시 무효화
- Google API 오류 시 최근 성공 캐시 사용 가능

절대 하지 말 것:

- 고객 브라우저에서 Google Sheets 직접 호출
- 페이지마다 Sheets API 재호출
- 이미지 파일을 Sheet에 직접 저장

---

# 4. Google Sheet

현재 Spreadsheet:

`일본 골프 여행 가이드 CMS - 도스·벳푸`

환경변수:

```env
GOOGLE_SHEET_ID=
GOOGLE_SERVICE_ACCOUNT_EMAIL=
GOOGLE_PRIVATE_KEY=
ADMIN_PASSWORD_HASH=
```

credential은 절대로 Git에 저장하지 않는다.

---

# 5. 골프장 데이터

탭:

`golf_courses`

필수 주요 값:

- id
- area
- 상품표명
- 공식명
- 주소
- 전화
- 코스요약
- 플레이/카트
- 클럽하우스 식사
- 목욕/샤워
- 렌탈
- 복장
- google_maps_url
- source_url
- status
- active
- sort
- last_verified

고객 화면:

`active = TRUE`만 표시.

---

# 6. 호텔 데이터

탭:

`hotels`

표시:

- 공식명
- 주소
- 전화
- 체크인
- 체크아웃
- 조식
- 목욕/스파
- 호텔 식사
- ATM/결제
- 교통
- 지도

---

# 7. 이동시간

탭:

`travel_times`

각 호텔과 각 골프장을 모든 조합으로 저장한다.

예:

```text
ANA Holiday Inn Tosu → 카오
ANA Holiday Inn Tosu → 위너스
ANA Holiday Inn Tosu → 포레스트낭칸
ANA Holiday Inn Tosu → 아마카세
ANA Holiday Inn Tosu → 그린랜드

Green Rich Hotel Tosu → 카오
...
```

웹에서는 다음처럼 표시:

```text
카오 골프장
예상 차량시간: 약 45~55분
[Google Maps 실시간 길찾기]
```

교통시간은 교통상황에 따라 변하므로 확정값처럼 표현하지 않는다.

---

# 8. 맛집

탭:

`restaurants`

관련 위치 연결:

- HOTEL
- GOLF
- AREA

필드:

- id
- area
- near_type
- near_id
- name
- category
- distance
- address
- hours
- price_range
- phone
- google_maps_url
- source_url
- status
- active
- sort
- last_verified

---

# 9. FAQ

탭:

`faq`

최종 필드:

```text
id
area
category
related_type
related_id
related_name
question_scope
question
answer
source_url
status
active
sort
```

---

# 10. FAQ 카테고리

반드시 아래 9개 고정:

```text
GOLF
HOTEL
ONSEN
DRIVER
RESTAURANT
GENERAL
REFUND
MONEY
EXTRA_PAYMENT
```

한국어 표시:

```text
골프장
호텔
온천
차량
맛집
기타
환불
환전
추가결제
```

---

# 11. 질문 범위

`question_scope`

## AREA

지역 공통 질문.

예:

- 도스 골프장은 고객이 지정 가능한가요?
- 일본에서 현금이 필요한가요?

## SPECIFIC

특정 장소 전용.

예:

- 그랜드 머큐어 대욕장은 몇 시까지인가요?
- 그린리치 조식은 몇 시인가요?

---

# 12. 관련 장소

`related_type`

```text
GOLF
HOTEL
RESTAURANT
```

특정 질문의 경우 반드시 related_id를 저장한다.

예:

```text
area = BEPPU
category = ONSEN
related_type = HOTEL
related_id = beppu_hotel_grandmercure
related_name = 그랜드 머큐어
```

---

# 13. 관리자 옵션

탭:

`admin_options`

이 탭을 관리자 UI의 설정값 원본으로 사용한다.

관리자 페이지 코드에 카테고리명을 하드코딩하지 않는다.

option_type:

- AREA
- CATEGORY
- SCOPE
- RELATED_TYPE

---

# 14. 관리자 기능

## 로그인

초기 MVP:

- 관리자 1개 비밀번호 또는 단일 관리자 인증
- 세션 쿠키
- 관리자 경로 보호

복잡한 권한 시스템 금지.

---

## 질문 목록

필터:

- 지역
- 카테고리
- 상태
- 검색

행:

- 질문
- 관련 장소
- 노출 상태
- 수정
- 숨김

---

## 질문 추가

관리자는 다음 순서로만 입력:

```text
지역
↓
질문 종류
↓
지역 공통 / 특정 장소
↓
특정 장소일 경우 장소 선택
↓
질문
↓
답변
↓
노출 여부
↓
저장
```

---

# 15. 관리자에게 숨길 값

아래는 시스템 내부 처리:

- id
- area code
- category code
- related_type code
- related_id
- sort number
- last_verified

일반 관리자 화면에는 한국어만 표시한다.

---

# 16. 정렬

관리자는 `sort` 숫자를 입력하지 않는다.

목록에서:

- 위로
- 아래로

또는 Drag & Drop.

저장 시 서버가 같은 그룹의 sort를 다시 1부터 순서대로 작성한다.

---

# 17. 숨김

삭제 대신:

```text
active = FALSE
```

권장.

관리자 UI에서는:

```text
[숨기기]
```

버튼으로 표현.

다시 노출:

```text
[다시 표시]
```

---

# 18. 오류 처리

Google Sheet 저장 실패:

```text
저장하지 못했습니다.
잠시 후 다시 시도해주세요.
```

절대 사용자에게 노출하지 말 것:

- API key
- service account email
- stack trace
- Google API raw error
- internal id

---

# 19. 데이터 정확성

`status`

예:

- 공식 확인
- 상품표 확인
- 공식+상품표 확인
- 확인 필요

모르는 내용은 절대로 임의 작성하지 않는다.

---

# 20. 관리자 저장 후

1. Google Sheet 저장
2. 성공 여부 확인
3. 관련 캐시 무효화
4. 관리자 목록 갱신
5. 성공 메시지

---

# 21. 검색

고객 FAQ 검색:

대상:

- question
- answer
- related_name

대규모 전문 검색엔진 불필요.

클라이언트 필터 또는 서버 단순 필터로 충분.

---

# 22. 성능 목표

모바일 4G 기준:

- 초기 화면 최대한 즉시 표시
- JS 번들 최소화
- 외부 라이브러리 최소화
- 관리자 코드 고객 번들에 포함하지 않기
- 이미지 lazy loading
- Google Maps embed 금지
- Maps는 링크 버튼 방식 우선

Google Map iframe은 필요한 경우에만 로드.

---

# 23. 보안

- 서비스 계정 credential 서버 전용
- 관리자 API 서버 전용
- 관리자 페이지 인증
- 요청 validation
- Sheet id 노출 자체는 보안수단으로 사용하지 않음
- 공개 쓰기 API 금지

---

# 24. 이모지 + 카테고리 색상 시스템

`src/lib/display.ts`에서 모든 이모지와 색상을 관리한다.

## 지역 이모지

```text
💮 DOS
🎋 BEPPU
🌏 ALL
```

## 카테고리 이모지 + 색상

각 카테고리는 고유 이모지와 색상(텍스트, 배경, 테두리)을 가진다.

9개 카테고리: GOLF, HOTEL, RESTAURANT, ONSEN, DRIVER, GENERAL, REFUND, MONEY, EXTRA_PAYMENT

## 적용 위치

- 고객 지역 홈: 카테고리 카드
- 고객 FAQ 페이지: 제목, accordion
- 관리자 카테고리 선택: 카드
- 관리자 FAQ 목록: 카드 border

---

# 25. 반응형 배경 이미지

## 파일 구조

```text
public/background/
├─ main-background-mobile.webp
├─ main-background-tablet.webp
├─ main-background-desktop.webp
├─ sub-background-1-mobile.webp
├─ sub-background-1-tablet.webp
├─ sub-background-1-desktop.webp
├─ sub-background-2-mobile.webp
├─ sub-background-2-tablet.webp
└─ sub-background-2-desktop.webp
```

## 페이지 매핑

| 페이지 | 배경 |
|---|---|
| 메인 (`/`) | main-background-* |
| 도스 (`/dos` + 하위) | sub-background-1-* |
| 벳푸 (`/beppu` + 하위) | sub-background-2-* |

## 반응형 로딩

CSS media query로 화면 크기에 따라 하나의 WebP만 로드한다.

```text
Mobile (기본): 768px 이하 → *-mobile.webp
Tablet (≥768px): 1280px 이하 → *-tablet.webp
Desktop (≥1280px): 1920px 이하 → *-desktop.webp
```

## Overlay

배경 위에 반투명 흰색 overlay를 적용한다.

```css
background: rgba(255, 255, 255, 0.78);
```

목적: 배경은 은은하게, 텍스트 가독성은 높게.

## 구현

- `globals.css`: `.page-bg`, `.bg-main`, `.bg-dos`, `.bg-beppu` 클래스
- `src/app/[area]/layout.tsx`: 지역별 배경 자동 적용
- `src/app/page.tsx`: 메인 페이지 배경 적용

## 원본

PNG 원본은 `background/` 폴더에 보관. 웹에서는 WebP만 사용.
